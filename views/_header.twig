{# Do not change this file name #}

<header class="site-header" data-visible="desktop">
{% for row in header.desktop if row.placements | length > 0 %}

  {% include 'header/_row.twig' %}

{% endfor %}
</header>